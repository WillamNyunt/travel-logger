import React from 'react'
import { useGetNotesByTripIdQuery } from 'src/slices/note'

export default function NoteModal() {

    
    // const { data, error, isLoading} = useGetNotesByTripIdQuery(1); 

  return (
    <div>
        NoteModal
    </div>
  )
}
